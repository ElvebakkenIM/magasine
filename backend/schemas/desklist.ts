import {defineField, defineType} from 'sanity'

export default {
    name: 'navigationItem',
    title: 'Navigation Item',
    fields: [
      
    ],
    preview: {
      select: {
        title: 'route.slug.current',
      }
    }
  }